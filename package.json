{
  "engines": {
    "node": "16.x" // Vue 2's last supported version
  },
  "scripts": {
    "build": "vue-cli-service build --modern --no-module",
    "vercel-build": "npm install && npm run build"
  }
}
